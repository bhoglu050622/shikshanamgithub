/**
 * Authentication Hook
 * Provides authentication state and methods
 * Re-exports the existing useAuth from auth-context
 */

export { useAuth } from '../auth-context';

// Note: UseAuthReturn type is not exported from auth-context
